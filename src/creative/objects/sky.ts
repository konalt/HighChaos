import { d, w, h } from "../../engine/engine";
import * as parallax_city from "./parallax_city";

export function draw() {
    d.rect(0, 0, w, h, "#239cff");
    /* parallax_city.draw(9);
    parallax_city.draw(5); */
}
